pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.go"
      run: golangci-lint run --fix
      stage_fixed: true

commit-msg:
  scripts:
    "conventional_commits":
      runner: bash
