/bin/
/vendor/
*.env
k8s/local/secret.yaml
terraform/local/terraform.tfvars

# Terraform local cache (downloaded provider binaries, platform-specific)
# Regenerated by "terraform init", so never commit.
.terraform/

# Terraform state files (contain secrets, never commit)
*.tfstate
*.tfstate.*